import ReactDOM from "react-dom";
import Structure from "./views/components/structure";
// theme components

const rootElement = document.getElementById("root");
ReactDOM.render(<Structure />, rootElement);
